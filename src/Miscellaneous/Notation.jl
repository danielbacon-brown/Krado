Notation:

Side 1 refers to the first layer in the stack (-z direction)
Side 2 refers to the last layer in the stack (+z direction)

# 𝐔 = 2- or 3-vector
U̅ = 2- or 3-vector
# L̅ = matrix



uᵢ = sequence

mnᵢ = 1D Array of (m,n) pairs
iₘₙ = index of each (m,n)

x_uᵢvᵢ = 2D array of elements corresponding to uᵢ and vᵢ
_mnᵢ = 1D array of elements corresponding to the harmonic order

δ = dimension
G̅_mnᵢδ = 2D array of G-vectors, where first dimension is the harmonic order and 2nd demensions i the physical dimension (x and y)

# Uᵢ = index of fractions of 𝐔 across unit cell. e.g. [ 0, 0.1, 0.2 ... 0.9]

len_ = the number of elements is in the following sequence
i_ = an index of the following seqence

indᵢ_ = a mapping of the values of the following sequence to the indices of that sequence

max_ = max value of the following

x_UᵢVᵢ = x-coordinate as indexed by Uᵢ and Vᵢ indices

CAPS = constant (even across scripts)
XDIM = 0
YDIM = 1
ZDIM = 2



# compared to papers
ℱϵ_ΔmᵢΔnᵢ = a
Cϵ = ⟦ϵ⟧
